# Add this to your i3status-rust config. The daemon command sink calls
# busctl on every deduped keyboard state change, and i3status-rust
# refreshes this block immediately through its custom_dbus API.

[[block]]
block = "custom_dbus"
path = "/qmk_state"
format = " $text "
