[project]
name = "svalboard-alex-tools"
version = "0.1.0"
description = "Alex's svalboard keymap tooling — Vial custom-QSID CLI and friends"
requires-python = ">=3.10"
dependencies = [
    "hidapi>=0.14",
]

[tool.uv]
package = false
